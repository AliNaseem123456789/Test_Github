#initialize Git
git init
git add .
git commit -m "commit"
git remote add origin https://github.com/ALi
git branch -M main
git push -u origin main

#check branch
git branch

#create bew branch
git branch Third_branch

#create new Branch AND swithc to it
git checkout -b feature/login

#push to new branch(use git add . and git commit first)
git push -u origin feature/login
